# Databricks App — Pricing Data Ingestion Review

resources:
  apps:
    pricing_ingestion_review:
      name: "pricing-ingestion-review"
      description: "HITL review and approval of external pricing data before merging into the Unified Pricing Table"
      source_code_path: ../src/app
      # Note: Databricks Apps don't auto-stop. Run `databricks apps stop pricing-ingestion-review`
      # when not demoing. Start with `databricks apps start pricing-ingestion-review`.

      resources:
        - name: sql-warehouse
          description: SQL warehouse for data queries
          sql_warehouse:
            id: ab79eced8207d29b
            permission: CAN_USE
