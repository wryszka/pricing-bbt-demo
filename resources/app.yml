# Databricks App — Pricing Workbench

resources:
  apps:
    pricing_workbench:
      name: "pricing-workbench"
      description: "End-to-end commercial pricing workbench — data ingestion HITL, feature store, model factory, quote review, governance"
      source_code_path: ../src/app
      # Note: Databricks Apps don't auto-stop. Run `databricks apps stop pricing-workbench`
      # when not demoing. Start with `databricks apps start pricing-workbench`.

      resources:
        - name: sql-warehouse
          description: SQL warehouse for data queries
          sql_warehouse:
            id: ab79eced8207d29b
            permission: CAN_USE
